# nisan-shemaryahu — Context

**Date:** 2026-07-07

## Goal
דף נחיתה ממיר + קמפיין ממומן + SEO/AEO חזק לניסן שמריהו — הנדימן, אינסטלטור ומנעולן באור יהודה והסביבה. שלושה דפי נחיתה נפרדים (אחד לכל שירות), 3 קמפיינים מקבילים באותו תקציב, אופטימיזציה לפי מה שמביא הכי הרבה לידים בפועל.

## Client / Owner
ניסן שמריהו — חבר של עומרי כתב. שיווק בפועל (הקמת חשבונות Google Ads/Meta) מבוצע ע"י אדיר.

## Stack
Next.js 16 (App Router, Turbopack), Framer Motion, Tailwind v4, Supabase (לידים).

## Success Criteria
- 3 עמודי שירות חיים ומומרים: `/מנעולן`, `/אינסטלטור`, `/הנדימן` (רוט ASCII פנימי: locksmith/plumbing/handyman + rewrites)
- CPL מתחת ל-60₪, זיהוי איזה משירות מביא הכי הרבה לידים
- אין תביעות מטעות (בלי "מוסמך" באינסטלציה עד שהתעודה בפועל תגיע, בלי "אחריות בכתב")

## Constraints
- תעודת אינסטלטור טרם התקבלה — לא להציג "מוסמך" ליד אינסטלציה
- אין ביקורות אמיתיות עדיין — לא להציג כרטיסי ביקורת מזויפים
- Turbopack לא מצליח לבנות סטטית עמודי ראוט עם שם תיקייה עברי — פתרון: תיקיות ASCII (`app/locksmith` וכו') + `rewrites()` ב-`next.config.ts` לשמירת ה-URL בעברית

## Design References
- כיוון עיצוב בהיר בהשראת ביו ישראל (bioisrael.co.il) — לא שכפול צבעים מדויק. פלטה חדשה: `--color-l-*` ב-`app/globals.css`, בשימוש רק בעמודי השירות (`.light-page`), לא נוגעת בעמוד הבית הכהה הקיים.

## Discovery Notes
שאלון איפיון מלא (14 סקציות) הושלם עם ניסן ב-6.7.2026 — כל התשובות, ניתוח מתחרה (ביו ישראל) ותכנית הרכש המקורית מתועדים ב-`docs/nisan-full-brief.html`. חבילות הקמפיין הסופיות ב-`docs/campaign-packages.md`. ר' `tasks.md` למה שנשאר.
