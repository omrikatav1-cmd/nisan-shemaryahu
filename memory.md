# nisan-shemaryahu — Local Memory

Project-scoped memory Claude uses when working inside this folder.

## Key Facts
- ניסן = חבר של עומרי כתב. איכות גבוהה מאוד חשובה כאן.
- מותג אישי (ניסן שמריהו), לא מותג שירותי — 3 verticals תחת אותו מותג.
- תעודת אינסטלטור עוד לא בידיים (~2 חודשים) — Hard constraint: לעולם לא "מוסמך" ליד אינסטלציה עד עדכון מפורש.
- מנעולן מוסמך + אישור משטרה — כן קיים ומאומת, מותר להציג.
- אין אחריות בכתב, אין מילואים — לעולם לא לטעון את זה.
- שיווק בפועל (הקמת/ניהול קמפיינים) מבוצע ע"י אדיר, לא על ידי הבנייה כאן.

## Decisions
- 3 דפי נחיתה נפרדים (לא עמודי עיר×שירות) — decision מ-6.7.2026, פישוט מהתכנון המקורי (שהיה 6-8 עמודי עיר).
- Turbopack (Next 16.2.1 בפרויקט הזה) לא מצליח לבנות סטטית ראוטים עם שם תיקייה בעברית — פתרון קבוע: תיקיות ASCII + `rewrites()` עם source מקודד ב-percent-encoding (לא ליטרל עברי גולמי — גם זה נכשל, ר' next.config.ts).
- פלטת עיצוב בהירה חדשה נוספה כ-`--color-l-*`, נפרדת לגמרי מהפלטה הכהה הקיימת — לא לגעת בעמוד הבית הכהה.

## Gotchas
- `middleware.ts` הוחלף ל-`proxy.ts` — הפרויקט הזה (AGENTS.md) מזהיר במפורש שזו לא ה-Next.js הרגילה, יש לבדוק docs לפני הנחות.
- `<a>` פנימי חייב `next/link` (ESLint אוכף `@next/next/no-html-link-for-pages`).
- אזהרה: אל תוסיפו `useEffect(() => ref.current?.focus())` על שדה טופס רחוק מלמעלה — גורם לדפדפן לגלול אוטומטית לשם בטעינה (נתפס דרך snapshot/DOM metrics, לא screenshot — כלי ה-screenshot נתן תוצאה שגויה/מטעה בסשן הזה, ה-accessibility snapshot היה אמין יותר לאבחון).
